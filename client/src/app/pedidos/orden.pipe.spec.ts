/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { OrdenPipe } from './orden.pipe';

describe('OrdenPipe', () => {
  it('create an instance', () => {
    let pipe = new OrdenPipe();
    expect(pipe).toBeTruthy();
  });
});
